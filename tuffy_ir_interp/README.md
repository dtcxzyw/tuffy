# tuffy_ir_interp

Miri-like IR interpreter with undefined behavior detection.

This crate provides an interpreter for `tuffy_ir` programs that can execute IR directly and detect UB violations (annotation violations, division by zero, out-of-bounds access, etc.).

## Status

Stub — implementation not yet started.

## Dependencies

- `tuffy_ir` — IR definitions to interpret
